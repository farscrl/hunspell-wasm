{
  "name": "@farscrl/hunspell-wasm",
  "version": "1.0.1",
  "description": "WebAssembly bindings for the Hunspell spellchecker, with a build pipeline included",
  "type": "module",
  "license": "MIT",
  "author": "far ScRL",
  "keywords": [
    "hunspell",
    "spellchecker",
    "wasm",
    "webassembly"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/farscrl/hunspell-wasm.git"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.29.3",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "imports": {
    "#hunspell-glue": {
      "types": "./src/lib/hunspell.d.mts",
      "browser": "./dist/lib/hunspell.web.mjs",
      "default": "./dist/lib/hunspell.mjs"
    }
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate:version": "node scripts/generate-hunspell-version.mjs",
    "build:wasm": "DOCKER_BUILDKIT=1 docker build -f native/Dockerfile --target export --output type=local,dest=src/lib .",
    "prebuild": "pnpm run generate:version",
    "build": "tsup && pnpm run build:lib",
    "build:lib": "mkdir -p dist/lib && cp src/lib/hunspell.mjs src/lib/hunspell.web.mjs dist/lib/",
    "pretypecheck": "pnpm run generate:version",
    "typecheck": "tsc --noEmit",
    "prelint": "pnpm run generate:version",
    "lint": "eslint .",
    "format": "prettier --write .",
    "pretest": "pnpm run generate:version && pnpm run build:lib",
    "test": "vitest run",
    "test:watch": "vitest",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0",
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
